It's a dating site where you choose from 2 options.

Important scripts are saved in the file. To run it, you need to create an empty Spring project and move the files to their places. Dependencies: MySQL Driver, Spring Data JDBC, Spring Web, Thymeleaf, Spring Boot DevTools.
